# unweighted, directed, acyclic graph


def earliest_ancestor(graph, vertex):
    """Returns the ancestor with the farthest distance and lowest numeric ID from the supplied vertex. If none is found, returns -1."""

    earliest = None
    return earliest
